<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Recurso</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Detalles del Recurso</h1>
    <p><strong>Título:</strong> <%= recurso.title %></p>
    <p><strong>Autor:</strong> <%= recurso.author %></p>
    <p><strong>Categoría:</strong> <%= recurso.category %></p>
    <p><strong>ISBN:</strong> <%= recurso.isbn %></p>
    
    <% if (recurso.image_path) { %>
        <p><strong>Imagen de Portada:</strong></p>
        <img src="/uploads/<%= recurso.image_path %>" alt="Portada" width="150">
    <% } %>

    <a href="/recursos">Volver a la lista</a>
</body>
</html>
